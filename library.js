/* =====================================================================
   라이브러리 (테스트 환경: 더미/시드 데이터 없음 — 빈 상태로 시작)
   · 템플릿/요소는 Figma 플러그인으로 등록 → /api/library(원격) 에서 불러옴
   · 앱은 시작 시 GET /api/library 로 원격 등록분을 가져와 채움
   ===================================================================== */
(function(){
  window.LIBRARY = {
    sizes: { '16:9':{w:1920,h:1080,inW:13.333,inH:7.5}, '4:3':{w:1024,h:768,inW:10,inH:7.5} },
    templateCategories: ['전체','사업계획서','IR Deck','제안서','기타'],
    elementCategories:  ['전체','통계','텍스트','강조','도형','리스트','기타'],
    templates: [],   // 더미 없음 — 플러그인 등록분으로 채워짐
    elements:  [],   // 더미 없음 — 플러그인 등록분으로 채워짐
  };
})();
